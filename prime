IDENTIFICATION DIVISION.
PROGRAM-ID. HELLO-WORLD.
AUTHOR .HAWIN.
ENVIRONMENT DIVISION.

DATA DIVISION.
WORKING-STORAGE SECTION.
01 WS-N PIC 9(3) VALUE 1.
01 WS-I PIC 9(3).
01 WS-PRIME PIC X VALUE 'Y'. 


PROCEDURE DIVISION.
PERFORM VARYING WS-N FROM 2 BY 1 UNTIL WS-N > 200
  MOVE 'Y' TO WS-PRIME
  PERFORM VARYING WS-I FROM 2 BY 1 UNTIL WS-I>(WS-N/2)
    IF FUNCTION MOD(WS-N WS-I)= 0
      MOVE 'N' TO WS-PRIME
      EXIT PERFORM
    END-IF
  END-PERFORM
  IF WS-PRIME = 'Y'
    DISPLAY WS-N 
  END-IF
 END-PERFORM.


STOP RUN.
